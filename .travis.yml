sudo: required
services: docker
language: minimal

install:
  - sudo apt-get install socat
  - ./minikube/ubuntu-compile-nsenter.sh

before_script:
  - ./minikube/minikube-create.sh

script:
  - ./minikube/launch.sh
