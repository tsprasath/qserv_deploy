apiVersion: v1
kind: ConfigMap
metadata:
  name: configmap-dot-lsst
data:
  {{ $root := . }}  
  {{ range $path, $bytes := .Files.Glob "configmap/dot-lsst/*" }}
  {{ base $path }}: '{{ $root.Files.Get $path }}'
  {{ end }}
