#!/bin/bash

# Copy orchestration script to kubernetes master
# Start Qserv pods
# Wait for Qserv startup

# @author Fabrice Jammes SLAC/IN2P3

set -e

DIR=$(cd "$(dirname "$0")"; pwd -P)

qserv-kubectl -C "start.sh"
qserv-kubectl -C "wait-pods-start.sh"
